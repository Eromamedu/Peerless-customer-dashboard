import { afterEach } from "vitest";

afterEach(() => {
  // Keeps each test isolated from the previous test.
  // Add cleanup logic here later if the application
  // starts using rendered components or mocked services.
});